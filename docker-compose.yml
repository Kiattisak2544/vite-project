version: "3.9"

services:
  web:
    build: .
    image: login-app:latest
    container_name: login-app
    ports:
      - "80:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3
